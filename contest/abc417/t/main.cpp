#include <iostream>
#include <vector>

#ifdef LOCAL
#include "dbg.h"
#else
#define dbg(...)
#endif

using namespace std;

struct Solver {
    Solver(istream& in, ostream& out) {

    }
};

int main() {
    Solver solver(cin, cout);
    return 0;
}
