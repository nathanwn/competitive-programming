#!/usr/bin/env bash

for((i = 1; ; ++i)); do
    echo '=================='
    if [[ ! -f "$i.in" ]] ; then
        echo 'Done.'
        exit 0
    fi
    echo "# Test $i"
    ./main < "$i.in" | tee "$i.out"
    if [[ ! -f "$i.ans" ]] ; then
        diff -w "$i.out" "$i.ans" || break
    fi
done
